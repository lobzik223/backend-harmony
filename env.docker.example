# ============================================================
# Harmony Backend — пример .env для Docker
# Скопируйте: cp env.docker.example .env
# На СЕРВЕРЕ в production ОБЯЗАТЕЛЬНО замените все секреты!
# Подробно: ENV-SERVER.md
# ============================================================

NODE_ENV=production
PORT=3000
API_PREFIX=/api

# ---- JWT (в production замените на свои, min 32 символа) ----
JWT_ACCESS_SECRET=harmony-local-jwt-access-secret-min-32-chars
JWT_REFRESH_SECRET=harmony-local-jwt-refresh-secret-min-32-chars

# ---- Ключ приложения: в приложении тот же ключ в заголовке X-Harmony-App-Key (production: min 32 символа) ----
APP_KEY=harmony-local-app-key-min-8

# ---- CORS: в production укажите домен приложения, не * ----
CORS_ORIGIN=*

# ---- Лимиты запросов (защита от брутфорса и DoS) ----
THROTTLE_TTL_SECONDS=60
THROTTLE_LIMIT=30

# ---- БД: пароль и URL (в production замените POSTGRES_PASSWORD на свой, min 24 символа) ----
POSTGRES_PASSWORD=harmony_secret
DATABASE_URL=postgresql://harmony:harmony_secret@postgres:5432/harmony

# ---- Ключ для запросов с сайта (production: свой ключ min 24 символа) ----
SITE_API_KEY=harmony-site-secret-key-change-me
SUBSCRIPTION_SITE_URL=https://harmony.app/premium
SUPPORT_TELEGRAM_URL=https://t.me/harmony_support
