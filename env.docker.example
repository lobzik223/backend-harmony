# Скопируйте в .env для запуска в Docker: copy env.docker.example .env
# Бэкенд + PostgreSQL в контейнерах. Регистрация и вход строго в БД.

NODE_ENV=production
PORT=3000
API_PREFIX=/api

# JWT — минимум 32 символа (в проде замените на свои)
JWT_ACCESS_SECRET=harmony-local-jwt-access-secret-min-32-chars
JWT_REFRESH_SECRET=harmony-local-jwt-refresh-secret-min-32-chars

APP_KEY=harmony-local-app-key-min-8

CORS_ORIGIN=*
THROTTLE_TTL_SECONDS=60
THROTTLE_LIMIT=30

# В docker-compose DATABASE_URL задаётся автоматически (postgres в той же сети).
# Для локального запуска без Docker задайте, например:
# DATABASE_URL=postgresql://harmony:harmony_secret@localhost:5432/harmony
DATABASE_URL=postgresql://harmony:harmony_secret@localhost:5432/harmony

SITE_API_KEY=harmony-site-secret-key-change-me
SUBSCRIPTION_SITE_URL=https://harmony.app/premium
SUPPORT_TELEGRAM_URL=https://t.me/harmony_support
